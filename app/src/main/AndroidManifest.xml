<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alamat.todolistapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_logo2"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoListApp">
        <activity android:name=".y.display_task_full_page"></activity>
        <activity android:name=".activities_fragments.InsertNewTodoActivity" />
        <activity android:name=".activities_fragments.Create_new_menu_Item_Activity" />
        <activity android:name=".y.DisplayActivity" />
        <activity android:name=".activities_fragments.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>